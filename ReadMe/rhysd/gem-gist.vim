## Use `jist` or `gist` rubygem in Vim

- `:Gist` uploads a current file to gist.github.com.
- `:GistPrivate` uploads a current file to gist.github.com as a private gist.

If "!" is added, the URL is opened with a browser.

`gem install jist` or `gem install gist` is required in advance.
