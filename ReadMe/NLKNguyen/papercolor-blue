# papercolor-blue
// Work-In-Progress

Example color scheme plugin for [PaperColor-Theme](https://github.com/NLKNguyen/papercolor-theme)

All features are as in PaperColor-Theme


## Installation

*Manually:*
Install [PaperColor-Theme](https://github.com/NLKNguyen/papercolor-theme)

Then place `PaperColor_blue.vim` file from this repository into `autoload` folder within your Vim directory, e.g. `~/.vim/autoload/`

*Using a plugin manager like [Vundle](https://github.com/gmarik/Vundle.vim)* (recommended for easy `:PluginUpdate`):

    Plugin 'NLKNguyen/papercolor-theme' " required
    Plugin 'NLKNguyen/papercolor-blue'

Then, put this in your `~/.vimrc`

```VimL
let g:PaperColor_Theme = 'blue'
colorscheme PaperColor
```
