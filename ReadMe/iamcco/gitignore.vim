# denite source for gitignore

> gitignore templates from https://github.com/dvcs/gitignore

### usage

`Denite gitignore`

#### default action `append`

select item and type `Enter` will trigger the action `append`
to create .gitignore or append content to the extists one
