textobj-headwordofline
===

This is Vim plugin to provide text objects to select the head word of line.


Screenshot
---

![screenshot](http://gifzo.net/0RVabLdLmX.gif)


Install
---

This plugin depends on [vim-textobj-user](http://github.com/kana/vim-textobj-user).

So you need to install [vim-textobj-user](http://github.com/kana/vim-textobj-user) together.


Usage
---

Default key mappings is `ih` and `ah` (operator mode).

`ih` can use inner head word of line(exclude space) as text object.

`ah` can use a head word of line(include space) as text object.

For example `dih` to use delete inner head word of line(exclude space).

